package cs3500.controller.strategy;

import java.util.List;

import cs3500.model.Card;
import cs3500.model.GameGrid;
import cs3500.model.Player;

/**
 * Class that finds the best position for the strategy of minimizing the max move the opponent can make.
 */
public class MinMaxStrategy implements ThreeTriosStrategy {


  @Override
  public List<int[]> choosePosition(GameGrid model, Player player) {
    return null;
  }
}
