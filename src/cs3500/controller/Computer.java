package cs3500.controller;

import cs3500.model.Card;

public interface Computer<C extends Card> {

  /**
   * Finds the play that flips the most amount of cards.
   * @return an array of size 2 that contains an x and y coordinate.
   */
//  int[] flipMostCards();

  /**
   * Finds the play that flips the most amount of cards.
   * @return the card that is best to play in the corner.
   */
//  C goForCorners();
}
